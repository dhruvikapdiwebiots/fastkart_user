


class HomeApiClass {
  loadLocalRecipe() async {
    /*try {
      String response =
          await rootBundle.loadString('assets/jsonFile/bannerlist.json');
      List result = json.decode(response);
      List<BannerModel> bannerList = [];
      for (var i = 0; i < result.length; i++) {
        BannerModel bannerModel = BannerModel.fromJson(result[i]);
        bannerList.add(bannerModel);
      }
      return bannerList;
    } catch (e) {
      throw const Padding(
        padding: EdgeInsets.only(top: 50),
        child: Center(
          child: Text('Convert Error'),
        ),
      );
    }*/
  }
}
